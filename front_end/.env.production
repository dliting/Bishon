# Bishon V2 frontend — production build environment.
# Used by `vite build` (defaults to --mode production).
# These values are baked into the dist at build time.

VITE_APP_API_HOST=
VITE_APP_API_PREFIX=/api
VITE_APP_WEB_PREFIX=/bishon
VITE_APP_MODE=prod
