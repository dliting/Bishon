# Bishon V2 — 主进程不依赖 Docker；离线 Docker 部署见 docs/deployment.md

# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# 向量存储
# - 默认装 CPU 版（小）。要 GPU 加速：pip uninstall faiss-cpu &&
#   pip install faiss-gpu-cu12  （需匹配 torch 的 CUDA 次版本，cu12 系均可）
faiss-cpu>=1.7.4

# HTTP 客户端（用于 Embedding/LLM API 调用）
httpx>=0.27

# 环境配置
python-dotenv>=1.0

# LLM 客户端 (OpenAI 兼容 API)
openai>=1.6
tiktoken>=0.4.0

# 文档解析
langchain>=1.0
langchain-community>=0.4
langchain-text-splitters>=1.1
unstructured>=0.11
python-magic-bin>=0.4; sys_platform == 'win32'
python-magic>=0.4; sys_platform != 'win32'
PyMuPDF>=1.23
opencv-python>=4.8
beautifulsoup4>=4.12
docx2txt>=0.8

# OCR（模型自动下载到 models/paddleocr_models/）
paddlepaddle>=3.0
paddleocr>=3.0

# Rerank 模型（进程内加载，Qwen3-Reranker-0.6B）
transformers>=4.51
# torch：默认 PyPI 拉到的是最新 CUDA 13 wheel。WSL + Windows driver ≤ 561.x
# 只支持 CUDA 12.6。手动指定 cu126 wheel：
#   pip install --index-url https://download.pytorch.org/whl/cu126 torch==2.12.0
# 验证：python -c "import torch; print(torch.cuda.is_available())"
torch>=2.0

# 通用工具
tqdm>=4.66
numpy>=1.23
pandas>=2.0
openpyxl>=3.1
python-pptx>=1.0
python-docx>=1.0
